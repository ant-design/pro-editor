import { randomIdcard } from './idcard';

test('randomIdcard', () => {
  expect(randomIdcard().length).toEqual(18);
  expect(randomIdcard().length).toEqual(18);
  expect(randomIdcard().length).toEqual(18);
  expect(randomIdcard().length).toEqual(18);
  expect(randomIdcard().length).toEqual(18);
  expect(randomIdcard().length).toEqual(18);
  expect(randomIdcard().length).toEqual(18);
  expect(randomIdcard().length).toEqual(18);
  expect(randomIdcard().length).toEqual(18);
  expect(randomIdcard().length).toEqual(18);
});
