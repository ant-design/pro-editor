export type { IconUnit, IconfontIcon, ReactIcon } from '@alipay/c2d2c-types';

/**
 * 外部的图标 url
 */
export interface ExternalScripts {
  /**
   * 名称
   */
  name: string;
  /**
   * URL
   */
  url: string;
}
