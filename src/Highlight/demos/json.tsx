/**
 * title: JSON 格式
 */

import { Highlight } from '@ant-design/pro-editor';

export default () => (
  <Highlight language="json">
    {{
      name: 'ProComponents',
      description: '基于 Ant Design 的 UI 资产库。',
      versions: ['1.0.0', '2.0.0', '2.5.1'],
    }}
  </Highlight>
);
